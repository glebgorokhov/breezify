import responsiveWidth from "responsive-width";
import clsx from "clsx";
const header = document.querySelector('.i');
header.classList.add('g r');
document.addEventListener('g', () => {
  document.getElementById("dynamicElement").className = clsx('q', {
    'g': window.innerWidth > 768,
    't': true,
    'x': document.body.classList.contains('g'),
    'j': true,
    z: false
  });
});
const mySpecialClass = 'q';
const decorated = 'z';
const classes = {
  z: 'z'
};
classes.decorated = 'z';
localStorage.setItem('z', 'z');
localStorage.getItem('z');
header.addClassName(() => {
  return () => ({
    class: {
      'g': responsiveWidth > 768
    },
    getClass: () => `q ${mySpecialClass} ${decorated} j`,
    'q': 'j'
  });
});
class Class {
  constructor() {
    this['g'] = 'g';
    this.decorated = 'q';
    this['z'] = 'z';
    this['j'] = 'j';
  }
}
const classObj = new Class();
