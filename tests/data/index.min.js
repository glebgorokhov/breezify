import responsiveWidth from "responsive-width";
import clsx from "clsx";
const header = document.querySelector('.j[role=\"decorated\"],aside>.D');
const mySpecialClass = 'a';
const decorated = 'f';
const headerTwo = document.querySelector(`.j[role="decorated"]:not(.f) .D,${decorated},aside>.D,${mySpecialClass}`);
header.classList.add('b C');
document.addEventListener("responsive-width", () => {
  document.getElementById("dynamicElement").className = clsx('a', {
    'b': window.innerWidth > 768,
    'p': true,
    'c': document.body.classList.contains('b'),
    'o': true,
    decorated: false
  });
});
const classes = {
  decorated: 'f'
};
classes.decorated = 'f';
localStorage.setItem("decorated", "decorated");
localStorage.getItem("decorated");
header.addClassName(() => {
  return () => ({
    class: {
      'b': responsiveWidth > 768
    },
    getClass: () => `a ${mySpecialClass} ${decorated} o`,
    'a': 'o'
  });
});
class Class {
  constructor() {
    this['b'] = 'b';
    this.decorated = 'a';
    this['f'] = 'f';
    this['o'] = 'o';
  }
}
const classObj = new Class();
