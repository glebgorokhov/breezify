import responsiveWidth from "responsive-width";
import clsx from "clsx";
const header = document.querySelector('.w');
header.classList.add('i u');
document.addEventListener('i', () => {
  document.getElementById("dynamicElement").className = clsx('v', {
    'i': window.innerWidth > 768,
    'o': true,
    'a': document.body.classList.contains('i'),
    'z': true,
    k: false
  });
});
const mySpecialClass = 'v';
const decorated = 'k';
const classes = {
  k: 'k'
};
classes.decorated = 'k';
localStorage.setItem('k', 'k');
localStorage.getItem('k');
header.addClassName(() => {
  return () => ({
    class: {
      'i': responsiveWidth > 768
    },
    getClass: () => `v ${mySpecialClass} ${decorated} z`,
    'v': 'z'
  });
});
class Class {
  constructor() {
    this['i'] = 'i';
    this.decorated = 'v';
    this['k'] = 'k';
    this['z'] = 'z';
  }
}
const classObj = new Class();
